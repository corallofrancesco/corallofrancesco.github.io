import {readFile,writeFile,mkdir,rm,cp} from 'node:fs/promises';
import {fileURLToPath} from 'node:url';
const stylesheet=await readFile(new URL('../public/style.css',import.meta.url),'utf8');
const root=fileURLToPath(new URL('../',import.meta.url));
const config=JSON.parse(await readFile(root+'content/site.json','utf8'));
const entries=JSON.parse(await readFile(root+'content/entries.json','utf8'));
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const groups={blog:['Writing','Notes on what comes to my mind lately.','Articles are on the way. This will be a place for practical lessons from building with AI.'],ideas:['Ideas','Questions, early observations, and directions worth exploring.','A space for thinking in public. New ideas will appear here as they take shape.'],projects:['Projects','Side projects and experiments, built with an engineer’s curiosity.','Projects will be added here with their context, technical approach, and links to explore further.'],products:['Products','Tools that turn a useful idea into something people can use.','Nothing released here yet. Future products will have their own pages, with details and links to try them.']};
const url=value=>{const u=new URL(value);if(!['https:','http:'].includes(u.protocol))throw Error('Expected an HTTP(S) URL');return esc(u.href)};
url(config.domain);url(config.linkedin);if(config.github)url(config.github);
const seen=new Set();
for(const e of entries){if(!groups[e.type]||!/^([a-z0-9]+-)*[a-z0-9]+$/.test(e.slug)||!e.title||!e.summary||!Array.isArray(e.body))throw Error('Invalid entry: '+JSON.stringify(e));const key=e.type+'/'+e.slug;if(seen.has(key))throw Error('Duplicate entry '+key);seen.add(key);if(e.url)url(e.url);if(e.date&&!/^\d{4}-\d{2}-\d{2}$/.test(e.date))throw Error('Use YYYY-MM-DD dates');}
const published=entries.filter(e=>e.published===true).sort((a,b)=>(b.date||'').localeCompare(a.date||''));
await rm(root+'dist',{recursive:true,force:true});await mkdir(root+'dist',{recursive:true});await cp(root+'public',root+'dist',{recursive:true});
function shell(title,description,body,path='',depth=0,active=''){
const prefix=depth===-1?config.domain.replace(/\/$/,'')+'/':'../'.repeat(depth)||'./';
return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)}</title><meta name="description" content="${esc(description)}"><link rel="canonical" href="${url(config.domain.replace(/\/$/,'')+'/'+path)}"><meta property="og:type" content="website"><meta property="og:title" content="${esc(title)}"><meta property="og:description" content="${esc(description)}"><meta property="og:url" content="${url(config.domain.replace(/\/$/,'')+'/'+path)}"><meta name="theme-color" content="#141817"><link rel="icon" href="${prefix}favicon.svg" type="image/svg+xml">${depth===-1?`<style>${stylesheet}</style>`:`<link rel="stylesheet" href="${prefix}style.css">`}</head><body><a class="skip" href="#main">Skip to content</a><div class="wrap"><header><a class="brand" href="${prefix}"><span>FC /</span>${esc(config.name)}</a><nav aria-label="Main navigation"><a href="${prefix}#about">About</a>${['blog','projects'].map(g=>`<a href="${prefix}${g}/" ${g===active?'aria-current="page"':''}>${groups[g][0]}</a>`).join('')}</nav></header>${body}<footer><span>© ${new Date().getFullYear()} ${esc(config.name)}</span><span>Space engineer. Developer at heart.</span><a href="${url(config.linkedin)}">LinkedIn ↗</a></footer></div></body></html>`;
}
const home=`<main id="main"><div class="hero"><div><p class="eyebrow">${esc(config.role)} · ${esc(config.company)}</p><h1>Engineering AI.<br><em>Making it useful.</em></h1><p class="intro">I’m Francesco — a space engineer, developer, and AI leader. I work on turning generative AI into measurable value, with a hands-on focus on agents, coding, and automation.</p><div class="actions"><a class="button" href="#about">More about me <span aria-hidden="true">↘</span></a><a class="text-link" href="${url(config.linkedin)}">Connect on LinkedIn ↗</a></div></div><aside class="field-note" aria-label="Current focus"><span class="meta">Currently working on</span><h2>From promising prototypes<br>to everyday impact.</h2><p>Helping teams put AI to work, in close collaboration with Information Systems.</p><div class="note-footer"><span>Strategy ↔ engineering</span><span aria-hidden="true">[01]</span></div></aside></div><div class="strip" aria-label="Areas of experience"><span>Built on engineering</span><span>Python & ML</span><span>Generative AI</span><span>AI agents</span><span>Space systems</span></div><section><div class="section-heading"><span class="number">01 /</span><h2>Where I focus</h2></div><div class="focus-grid"><article><span class="meta">Build</span><h3>Agents & automation</h3><p>AI agents for coding and repetitive work. A hands-on approach to prototyping, testing, and understanding what is useful in practice.</p></article><article><span class="meta">Transform</span><h3>AI with a business case</h3><p>Country-level AI leadership focused on measurable return from generative AI, connecting technical possibilities with how teams actually work.</p></article><article><span class="meta">Explore</span><h3>Intelligence beyond Earth</h3><p>AI strategy and edge AI for spacecraft, informed by a background in aerospace engineering and space systems.</p></article></div></section><section id="about" class="about"><div class="section-heading"><span class="number">02 /</span><h2>An engineer’s<br>route into AI.</h2></div><div class="about-text"><p>I studied aerospace and space engineering. But I’ve always been a developer at heart: curious about how things work, and happiest when building something to find out.</p><p>Machine learning caught my attention in 2017. After joining a large space company as a systems engineer, I soon found myself writing Python and developing ML models within the engineering team.</p><p>That path led to my current role as ${esc(config.role)} at ${esc(config.company)}. Today, I focus on getting real value from generative AI: coding agents, automation, and adoption. My responsibility also includes AI strategy and edge AI for spacecraft.</p><p>I bring the same engineering mindset to transformation: understand the problem, build something useful, and measure what changes.</p></div></section><section><div class="section-heading"><span class="number">03 /</span><h2>Outside the day job</h2></div><div class="explore">${['blog','projects'].map(g=>`<a href="./${g}/"><div><h3>${groups[g][0]}</h3><p>${({blog:'Lessons and observations from working with AI.',projects:'Experiments, code, and things built on the side.'})[g]}</p></div><span class="arrow" aria-hidden="true">↗</span></a>`).join('')}</div><p class="quiet">${published.length?'An evolving collection of independent work.':'A growing space. Writing and independent work will appear here as I publish.'}</p></section><section class="contact"><div><h2>Let’s compare notes.</h2><p>On AI transformation, building useful agents, or bringing an engineering idea to life.</p></div><a class="button" href="${url(config.linkedin)}">Connect on LinkedIn ↗</a></section></main>`;
await writeFile(root+'dist/index.html',shell(config.name+' — AI leadership & engineering',config.description,home));
for(const [g,[label,description,empty]]of Object.entries(groups)){
 await mkdir(root+'dist/'+g,{recursive:true});const list=published.filter(e=>e.type===g);
 const body=`<main id="main" class="collection-main"><div class="page-head"><p class="eyebrow">Independent work / ${esc(label)}</p><h1>${label}</h1><p>${description}</p></div>${list.length?`<ul class="entry-list">${list.map(e=>`<li><span class="meta">${esc(e.date||label)}</span><h2><a href="./${e.slug}/">${esc(e.title)} ↗</a></h2><p>${esc(e.summary)}</p></li>`).join('')}</ul>`:`<div class="empty"><h2>${g==='products'?'Still at the workbench.':'More to come.'}</h2><p>${empty}</p><a class="back" href="../">← Back to home</a></div>`}</main>`;
 await writeFile(root+'dist/'+g+'/index.html',shell(label+' — '+config.name,description,body,g+'/',1,g));
 for(const e of list){await mkdir(root+`dist/${g}/${e.slug}`,{recursive:true});const body=`<main id="main"><a class="back" href="../">← All ${label.toLowerCase()}</a><div class="page-head"><p class="eyebrow">${label}${e.date?' / '+esc(e.date):''}</p><h1>${esc(e.title)}</h1><p>${esc(e.summary)}</p></div><article class="prose" aria-label="${esc(e.title)}">${e.body.map(block=>typeof block==='string'?`<p>${esc(block)}</p>`:block.heading?`<h2>${esc(block.heading)}</h2>`:'').join('')}${e.url?`<p><a href="${url(e.url)}">${esc(e.linkLabel||'Explore the project')} ↗</a></p>`:''}</article></main>`;await writeFile(root+`dist/${g}/${e.slug}/index.html`,shell(e.title+' — '+config.name,e.summary,body,`${g}/${e.slug}/`,2,g));}
}
await writeFile(root+'dist/404.html',shell('Page not found — '+config.name,'Page not found.',`<main id="main" class="collection-main"><div class="page-head"><p class="eyebrow">404</p><h1>Off the map.</h1><p>This page may have moved or hasn’t been published yet.</p><a class="button" href="${url(config.domain)}">Back to home</a></div></main>`,'404.html',-1));
const paths=['',...Object.keys(groups).map(g=>g+'/'),...published.map(e=>`${e.type}/${e.slug}/`)];
await writeFile(root+'dist/sitemap.xml',`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map(p=>`<url><loc>${esc(config.domain.replace(/\/$/,'')+'/'+p)}</loc></url>`).join('')}</urlset>`);
await writeFile(root+'dist/robots.txt',`User-agent: *\nAllow: /\nSitemap: ${config.domain.replace(/\/$/,'')}/sitemap.xml\n`);
await writeFile(root+'dist/.nojekyll','');
console.log(`Built ${paths.length+1} pages in dist/`);
